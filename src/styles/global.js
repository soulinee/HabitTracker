import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container:{
        flex: 1,
    },
    titleText:{
        fontFamily:"archivo",
       // fontWeight:"bold",
        fontSize:30,
        color:"#333"
    },
});